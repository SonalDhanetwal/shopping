server:
  port: 9094

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/test"
    username: postgres
    password: postgres
    platform: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    generate-ddl: true
    database: POSTGRESQL
